# 前端代码开发规范 V2.0

## 一、概述

本规范适用于公司所有前端项目的代码开发，技术栈为 Vue3 + TypeScript + Vite + Element Plus。

## 二、命名规范

### 2.1 文件命名
- 组件文件使用 PascalCase：`UserProfile.vue`、`OrderList.vue`
- 工具函数使用 kebab-case：`format-date.ts`、`validate-form.ts`
- 页面组件放在 `views/` 目录下，公共组件放在 `components/` 目录下

### 2.2 变量命名
- 普通变量和函数使用 camelCase：`userName`、`handleSubmit()`
- 常量使用 UPPER_SNAKE_CASE：`MAX_UPLOAD_SIZE`、`API_BASE_URL`
- 组件名使用 PascalCase：`UserProfile`、`DataTable`
- 类型/接口名使用 PascalCase：`UserInfo`、`ApiResponse<T>`

### 2.3 CSS命名
- 使用 BEM 命名法：`.block__element--modifier`
- 组件样式必须使用 `scoped` 属性，避免样式污染：`<style scoped>`

## 三、目录结构

```
src/
├── api/             # API接口层，按业务模块分文件
├── assets/          # 静态资源（图片、图标、全局样式）
├── components/      # 公共组件
├── composables/     # 组合式函数（hooks）
├── router/          # 路由配置
├── stores/          # Pinia状态管理
├── types/           # TypeScript类型定义
├── utils/           # 工具函数
├── views/           # 页面组件
├── App.vue
└── main.ts
```

## 四、组件规范

1. 每个组件不超过300行代码，超过则拆分子组件
2. 组件 Props 必须定义类型：`defineProps<{ title: string; count?: number }>()`
3. 禁止使用 `any` 类型，不确定类型时使用 `unknown`
4. 复杂逻辑抽取到 composables 中，保持组件简洁

## 五、代码格式

- 缩进使用2个空格
- 字符串使用单引号 `'hello'`
- 语句末尾加分号
- 使用 ESLint + Prettier 自动格式化，配置文件统一由项目脚手架提供

## 六、代码审查要点

1. 是否有类型错误
2. 是否有未处理异常
3. 是否存在内存泄漏（未清除的定时器、事件监听）
4. 接口请求是否处理了 loading 和 error 状态
5. 是否遵循了上述命名和结构规范
